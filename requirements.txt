# ── Core ──────────────────────────────────────────────────────
pandas>=2.0
tqdm>=4.60
playwright>=1.40
python-dotenv>=1.0
pymongo>=4.6
pyarrow>=14.0        # backend para .to_parquet()

# ── Visualizaciones (opcionales) ──────────────────────────────
# Necesarias solo para plot_heatmap() y plot_pass_map()
mplsoccer>=1.2
matplotlib>=3.7

# ── Modelado ──────────────────────────────────────────────────
lightgbm>=4.0
scikit-learn>=1.3
statsmodels>=0.14
joblib>=1.3
jupyter>=1.0
requests>=2.31      # weather_fetcher y futuras integraciones

# ── Post-install ──────────────────────────────────────────────
# Después de `pip install -r requirements.txt`, ejecutar también:
#   playwright install chromium
