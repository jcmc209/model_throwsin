# ── Entorno Python ────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
.eggs/

# ── Secretos ──────────────────────────────────────────────────
.env
# .env.example sí se sube (no contiene valores reales)

# ── Datos scrapeados (pesados, regenerables con whoscored_scraper.py) ──
data/whoscored_laliga/

# ── Artefactos del modelo (derivados, regenerables con model/train.py) ──
data/model/dataset.parquet
data/model/dataset_match.parquet
data/model/model_v1.joblib
data/model/model_v1_total.joblib
data/model/model_q25.joblib
data/model/model_q50.joblib
data/model/model_q75.joblib
data/model/optuna_study.db
data/model/predictions/
data/model/*.bak
# feature_importance.csv, metrics_*.json, shap_importance.csv y cv_results.* SÍ se suben (livianos)
data/model/market_eval/

# ── Datos de referencia voluminosos (se regeneran con weather_fetcher.py) ──
data/reference/weather.parquet

# ── Logs ──────────────────────────────────────────────────────
*.log
logs/
!logs/.gitkeep

# ── Jupyter checkpoints ───────────────────────────────────────
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# ── OS / IDE ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo

# ── Playwright ────────────────────────────────────────────────
playwright-report/
test-results/

# ── Infraestructura interna de Cursor ─────────────────────────
openspec/
.atl/
